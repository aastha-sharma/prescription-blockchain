peer:
  id: peer0
  networkId: dev
  listenAddress: 0.0.0.0:7051
  chaincodeListenAddress: 0.0.0.0:7052
  keepalive:
    minInterval: 60s
    client:
      interval: 60s
      timeout: 20s
    deliveryClient:
      interval: 60s
      timeout: 20s

chaincode:
  logging:
    level: info

ledger:
  state:
    stateDatabase: goleveldb
